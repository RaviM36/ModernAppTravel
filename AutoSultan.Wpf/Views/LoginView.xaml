<Window x:Class="AutoSultan.Wpf.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Icon="{x:Null}" Height="360" Width="520" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Background="#FFF2F2F2">
        <Border Width="420" Height="300" Background="White" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="56" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Background="#b71c1c" CornerRadius="4,4,0,0">
                    <Grid>
                        <TextBlock Text="AUTO SULTAN"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   FontSize="20"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   TextAlignment="Center"
                                   Margin="0" />
                    </Grid>
                </Border>

                <!-- Content -->
                <StackPanel Grid.Row="1" Margin="20" VerticalAlignment="Top">
                    <TextBlock Text="Username" Foreground="#666" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="28" Margin="0,0,0,8" />

                    <TextBlock Text="Password" Foreground="#666" FontWeight="SemiBold" Margin="0,8,0,6" />
                    <PasswordBox x:Name="PasswordBox" Height="28" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,12" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,16,0,0">
                        <Button Content="Login" Width="100" Height="34" Background="#b71c1c" Foreground="White" Command="{Binding LoginCommand}" Margin="0,0,8,0" />
                        <Button Content="Close" Width="100" Height="34" Background="#eee" Command="{Binding CloseCommand}" Margin="0,0,8,0" />
                        <Button Content="Sign in with Microsoft" Width="170" Height="34" Background="#2b5797" Foreground="White" Command="{Binding EntraSignInCommand}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>