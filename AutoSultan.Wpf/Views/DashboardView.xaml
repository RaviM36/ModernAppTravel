<Window x:Class="AutoSultan.Wpf.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auto Sultan - Dashboard" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <DockPanel>
        <!-- Top bar -->
        <Border Background="#f4f4f4" DockPanel.Dock="Top" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Username, StringFormat='Welcome, {0}'}" VerticalAlignment="Center" FontWeight="SemiBold" />

                <TextBlock Grid.Column="1" Text="{Binding CurrentDateTime}" Margin="12,0" VerticalAlignment="Center" />

                <Button Grid.Column="2" Content="Logout" Command="{Binding LogoutCommand}" Width="100" Height="34" Background="#b71c1c" Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Main content placeholder -->
        <Grid Background="White">
            <TextBlock Text="Dashboard content goes here" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#666" />
        </Grid>
    </DockPanel>
</Window>